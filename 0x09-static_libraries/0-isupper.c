#include "main.h"

/**
 * _isupper - function that checks for upper characters
 * gc: int type parameter
 * Return: 1 if uppercase, else 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= '2')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
